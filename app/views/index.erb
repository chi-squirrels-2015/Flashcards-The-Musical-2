<div class="container">
  <div id="login_register">

  </div>
  <br>
  <!--Here we will have all of the deck links displayed-->
  <a id="create_deck" href="/decks/new">Create Your Own!</a><br>
  <br>
  <% @decks.each do |deck| %>
  <!-- /games/:game_id/decks/:deck_id/cards/:id -->
    <a href="games/<%= @game.id + 1 %>/decks/<%= deck.id %>/cards/<%= @card.id %>"><%= deck.deck_name %></a>
    <p><%= deck.description %></p>
    <br>
  <% end %>
</div>


<!--Variables we will need: @decks which is a list of all the Deck objects-->
